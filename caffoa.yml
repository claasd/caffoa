config:
  duplicates: once # once, overwrite (default)
services:
  - apiPath: tests\openapi.yml

    model:
      namespace: My.Model
      targetFolder: ./output/Model
      prefix: Base
      suffix: ""

  - apiPath: tests\openapi.yml

    model:
      namespace: My.Model
      targetFolder: ./output/Model
      prefix: Base
      suffix: ""

